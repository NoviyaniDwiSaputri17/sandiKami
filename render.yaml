services:
  - type: web
    name: sandikami
    env: php
    plan: free
    buildCommand: "composer install && php artisan key:generate && php artisan config:clear"
    startCommand: "php artisan serve --host=0.0.0.0 --port=$PORT"